---
/**
 * Hero Section Component
 * Full-screen hero with premium GSAP animations, parallax, and mouse interactions
 * Inspired by luxury automotive brand landing pages
 */
import { Image } from 'astro:assets';
import heroPoster from '../../assets/videos/hero1-poster.jpg';
import { supabase } from '../../lib/supabase';

interface SlideText {
  eyebrow: string;
  titleLine1: string;
  titleLine2: string;
  titleLine2IsAccent: boolean;
  subtitle: string;
  ctaPrimaryText: string;
  ctaPrimaryHref: string;
  ctaSecondaryText: string;
  ctaSecondaryHref: string;
}

const FALLBACK_SLIDES: SlideText[] = [
  {
    eyebrow: 'SINCE 2010',
    titleLine1: '당신의 Brand를',
    titleLine2: 'Design하다',
    titleLine2IsAccent: true,
    subtitle: 'LED Signage · Neon Sign · Advertising Design - 당신의 브랜드를 가장 감각적으로',
    ctaPrimaryText: '쇼핑몰 둘러보기',
    ctaPrimaryHref: '/shop',
    ctaSecondaryText: '포트폴리오 보기',
    ctaSecondaryHref: '#portfolio',
  },
  {
    eyebrow: 'LED SIGNAGE',
    titleLine1: '빛으로 만드는',
    titleLine2: 'Brand Identity',
    titleLine2IsAccent: true,
    subtitle: '최신 LED 기술로 브랜드의 가치를 높이는 Signage Solution을 제공합니다',
    ctaPrimaryText: 'LED 간판 보기',
    ctaPrimaryHref: '/shop',
    ctaSecondaryText: '무료 상담 신청',
    ctaSecondaryHref: '#contact',
  },
  {
    eyebrow: 'NEON SIGN',
    titleLine1: 'Creative한',
    titleLine2: 'Neon Art',
    titleLine2IsAccent: true,
    subtitle: '감각적인 Neon Sign으로 공간에 특별한 Atmosphere를 더합니다',
    ctaPrimaryText: '네온사인 컬렉션',
    ctaPrimaryHref: '/shop',
    ctaSecondaryText: '시공 사례 보기',
    ctaSecondaryHref: '#portfolio',
  },
  {
    eyebrow: 'TOTAL SOLUTION',
    titleLine1: 'Design부터',
    titleLine2: '시공까지 One-Stop',
    titleLine2IsAccent: true,
    subtitle: '기획 · Design · 제작 · 시공 · A/S까지, 하나의 흐름으로 완성합니다',
    ctaPrimaryText: '견적 문의하기',
    ctaPrimaryHref: '#contact',
    ctaSecondaryText: '회사 소개',
    ctaSecondaryHref: '#about',
  },
];

let slides = FALLBACK_SLIDES;
try {
  const { data, error } = await supabase
    .from('hero_slides')
    .select('*')
    .eq('page', 'landing')
    .eq('is_visible', true)
    .order('slide_index');
  if (!error && data && data.length > 0) {
    slides = data.map((s: any) => ({
      eyebrow: s.eyebrow ?? FALLBACK_SLIDES[0].eyebrow,
      titleLine1: s.title_line1 ?? '',
      titleLine2: s.title_line2 ?? '',
      titleLine2IsAccent: true,
      subtitle: s.subtitle ?? '',
      ctaPrimaryText: s.cta_primary_text ?? '쇼핑몰 둘러보기',
      ctaPrimaryHref: s.cta_primary_href ?? '/shop',
      ctaSecondaryText: s.cta_secondary_text ?? '포트폴리오 보기',
      ctaSecondaryHref: s.cta_secondary_href ?? '#portfolio',
    }));
  }
} catch {}
---

<section id="home" class="hero" data-hero>
  <!-- Background Layer -->
  <div class="hero-background" data-hero-bg>
    <!-- Animated Gradient Background -->
    <div class="hero-gradient-layer" data-hero-gradient></div>
    
    <!-- Self-hosted Video Background (per-slide) -->
    <div class="hero-video-wrapper" data-hero-bg-wrapper>
      <!-- Mobile fallback image -->
      <Image
        src={heroPoster}
        alt="issac.design 히어로"
        class="hero-video-fallback"
        loading="eager"
        decoding="async"
        fetchpriority="high"
        width={1920}
        height={1080}
        layout="full-width"
        data-hero-fallback
      />
      <!-- Per-slide videos -->
      <video
        class="hero-video is-active"
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        poster="/videos/hero1-poster.jpg"
        data-hero-video="0"
      >
        <source src="/videos/hero1-opt.webm" type="video/webm" />
        <source src="/videos/hero1-opt.mp4" type="video/mp4" />
      </video>
      <video
        class="hero-video"
        muted
        loop
        playsinline
        preload="metadata"
        poster="/videos/hero2-poster.jpg"
        data-hero-video="1"
      >
        <source src="/videos/hero2-opt.webm" type="video/webm" />
        <source src="/videos/hero2-opt.mp4" type="video/mp4" />
      </video>
      <video
        class="hero-video"
        muted
        loop
        playsinline
        preload="metadata"
        poster="/videos/hero3-poster.jpg"
        data-hero-video="2"
      >
        <source src="/videos/hero3-opt.webm" type="video/webm" />
        <source src="/videos/hero3-opt.mp4" type="video/mp4" />
      </video>
      <video
        class="hero-video"
        muted
        loop
        playsinline
        preload="metadata"
        poster="/videos/hero4-poster.jpg"
        data-hero-video="3"
      >
        <source src="/videos/hero4-opt.webm" type="video/webm" />
        <source src="/videos/hero4-opt.mp4" type="video/mp4" />
      </video>
    </div>
    
    <!-- Overlay -->
    <div class="hero-overlay"></div>
    
    <!-- Noise Texture -->
    <div class="hero-noise"></div>
    
    <!-- Decorative Elements -->
    <div class="hero-grid"></div>
    
    <!-- Floating Geometric Elements -->
    <div class="hero-geometrics" data-hero-geometrics>
      <div class="geo-shape geo-shape--1" data-geo="1">
        <svg viewBox="0 0 100 100" fill="none">
          <circle cx="50" cy="50" r="48" stroke="currentColor" stroke-width="0.5"/>
          <circle cx="50" cy="50" r="35" stroke="currentColor" stroke-width="0.3"/>
        </svg>
      </div>
      <div class="geo-shape geo-shape--2" data-geo="2">
        <svg viewBox="0 0 100 100" fill="none">
          <rect x="10" y="10" width="80" height="80" stroke="currentColor" stroke-width="0.5" transform="rotate(45 50 50)"/>
        </svg>
      </div>
      <div class="geo-shape geo-shape--3" data-geo="3">
        <svg viewBox="0 0 100 100" fill="none">
          <polygon points="50,5 95,75 5,75" stroke="currentColor" stroke-width="0.5"/>
        </svg>
      </div>
      <div class="geo-shape geo-shape--4" data-geo="4">
        <svg viewBox="0 0 100 100" fill="none">
          <line x1="0" y1="50" x2="100" y2="50" stroke="currentColor" stroke-width="0.3"/>
          <line x1="50" y1="0" x2="50" y2="100" stroke="currentColor" stroke-width="0.3"/>
          <circle cx="50" cy="50" r="20" stroke="currentColor" stroke-width="0.5"/>
        </svg>
      </div>
      <div class="geo-shape geo-shape--5" data-geo="5">
        <svg viewBox="0 0 100 100" fill="none">
          <path d="M25,50 Q50,10 75,50 Q50,90 25,50" stroke="currentColor" stroke-width="0.5"/>
        </svg>
      </div>
    </div>
    
    <!-- Particle Dots -->
    <div class="hero-particles" data-hero-particles>
      {Array.from({ length: 30 }).map((_, i) => (
        <div class="particle" data-particle={i} />
      ))}
    </div>
    
    <!-- Mouse Follower Light -->
    <div class="hero-mouse-light" data-hero-mouse-light></div>
  </div>

  <!-- Content -->
  <div class="hero-content" data-hero-content>
    <!-- Slide Container -->
    <div class="hero-slides" data-hero-slides>
      {slides.map((slide, index) => (
        <div class={`hero-slide${index === 0 ? ' is-active' : ''}`} data-hero-slide={String(index)}>
          <div class="hero-content-inner">
            <span class="hero-eyebrow" data-hero-eyebrow>
              <span class="hero-eyebrow-line"></span>
              <span class="hero-eyebrow-text">{slide.eyebrow}</span>
            </span>
            <h2 class="hero-title" data-hero-title>
              <span class="hero-title-line">
                <span class="hero-title-text" data-split-text>{slide.titleLine1}</span>
              </span>
              <span class="hero-title-line">
                <span class={`hero-title-text${slide.titleLine2IsAccent ? ' hero-title-accent' : ''}`} data-split-text data-accent>{slide.titleLine2}</span>
              </span>
            </h2>
            <p class="hero-subtitle" data-hero-subtitle>
              <span class="hero-subtitle-text">{slide.subtitle}</span>
            </p>
            <div class="hero-cta" data-hero-cta>
              <a href={slide.ctaPrimaryHref} class="button button--primary hero-cta-btn" data-magnetic>
                <span class="btn-text">{slide.ctaPrimaryText}</span>
                <span class="btn-shine"></span>
                <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                  <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
              <a href={slide.ctaSecondaryHref} class="button button--outline hero-cta-btn" data-magnetic>
                <span class="btn-text">{slide.ctaSecondaryText}</span>
                <span class="btn-shine"></span>
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Slide Indicators -->
    <div class="hero-indicators" data-hero-indicators>
      {slides.map((_, index) => (
        <button class={`hero-indicator${index === 0 ? ' is-active' : ''}`} data-hero-indicator={String(index)} aria-label={`슬라이드 ${index + 1}`}>
          <span class="hero-indicator-progress"></span>
        </button>
      ))}
    </div>

  </div>

  <!-- Scroll Indicator -->
  <div class="scroll-indicator" data-scroll-indicator>
    <span class="scroll-indicator-text">SCROLL</span>
    <div class="scroll-indicator-line">
      <div class="scroll-indicator-dot"></div>
    </div>
    <svg class="scroll-indicator-arrow" width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
      <path d="M2 4l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </div>
  
  <!-- Luxury Brand Lines -->
  <div class="hero-brand-lines">
    <div class="brand-line brand-line--left"></div>
    <div class="brand-line brand-line--right"></div>
  </div>
</section>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    height: 100dvh;
    min-height: 600px;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  /* Background */
  .hero-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }
  
  /* Animated Gradient Layer */
  .hero-gradient-layer {
    position: absolute;
    inset: -50%;
    width: 200%;
    height: 200%;
    background: 
      radial-gradient(ellipse 80% 50% at 20% 40%, rgba(26, 54, 93, 0.4) 0%, transparent 50%),
      radial-gradient(ellipse 60% 40% at 80% 60%, rgba(79, 70, 229, 0.2) 0%, transparent 50%),
      radial-gradient(ellipse 50% 30% at 40% 80%, rgba(234, 179, 8, 0.15) 0%, transparent 50%);
    opacity: 0;
    will-change: transform;
  }
  
  /* Video Background */
  .hero-video-wrapper {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .hero-video {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    min-width: 100%;
    min-height: 100%;
    width: auto;
    height: auto;
    object-fit: cover;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.8s ease;
  }

  .hero-video.is-active {
    opacity: 1;
  }

  .hero-video-fallback {
    display: none;
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(0, 0, 0, 0.82) 0%,
      rgba(0, 0, 0, 0.55) 40%,
      rgba(0, 0, 0, 0.75) 100%
    );
  }

  .hero-noise {
    position: absolute;
    inset: 0;
    opacity: 0.04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
    pointer-events: none;
    mix-blend-mode: overlay;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 70% 50% at 50% 50%, black, transparent);
    pointer-events: none;
  }
  
  /* Floating Geometric Elements */
  .hero-geometrics {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }
  
  .geo-shape {
    position: absolute;
    color: rgba(255, 255, 255, 0.06);
    will-change: transform;
    opacity: 0;
    transform: scale(0);
  }
  
  .geo-shape svg {
    width: 100%;
    height: 100%;
  }
  
  .geo-shape--1 {
    top: 10%;
    right: 15%;
    width: 300px;
    height: 300px;
  }
  
  .geo-shape--2 {
    top: 60%;
    left: 5%;
    width: 200px;
    height: 200px;
  }
  
  .geo-shape--3 {
    bottom: 20%;
    right: 25%;
    width: 150px;
    height: 150px;
  }
  
  .geo-shape--4 {
    top: 30%;
    left: 20%;
    width: 180px;
    height: 180px;
  }
  
  .geo-shape--5 {
    top: 5%;
    left: 40%;
    width: 120px;
    height: 120px;
  }
  
  /* Particle Dots */
  .hero-particles {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }
  
  .particle {
    position: absolute;
    width: 3px;
    height: 3px;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 50%;
    opacity: 0;
    will-change: transform, opacity;
  }
  
  .particle::before {
    content: '';
    position: absolute;
    inset: -2px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.4) 0%, transparent 70%);
    border-radius: 50%;
  }
  
  /* Mouse Follower Light */
  .hero-mouse-light {
    position: absolute;
    width: 600px;
    height: 600px;
    background: radial-gradient(
      circle,
      rgba(79, 70, 229, 0.15) 0%,
      rgba(26, 54, 93, 0.1) 30%,
      transparent 70%
    );
    border-radius: 50%;
    pointer-events: none;
    opacity: 0;
    transform: translate(-50%, -50%);
    will-change: transform, opacity;
    mix-blend-mode: screen;
  }
  
  /* Luxury Brand Lines */
  .hero-brand-lines {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 2;
  }
  
  .brand-line {
    position: absolute;
    background: linear-gradient(
      180deg,
      transparent 0%,
      rgba(255, 255, 255, 0.1) 20%,
      rgba(255, 255, 255, 0.1) 80%,
      transparent 100%
    );
    width: 1px;
    height: 100%;
    opacity: 0;
  }
  
  .brand-line--left {
    left: 10%;
  }
  
  .brand-line--right {
    right: 10%;
  }

  /* Content */
  .hero-content {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-padding);
    padding-top: var(--header-height);
    padding-bottom: var(--space-4xl);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .hero-content-inner {
    max-width: 800px;
    margin: 0 auto;
    margin-top: -5vh;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  /* Eyebrow */
  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-eyebrow-line {
    width: 0;
    height: 1px;
    background: var(--primary-light);
    will-change: width;
  }

  .hero-eyebrow-text {
    font-size: var(--font-size-xs);
    font-weight: 500;
    letter-spacing: var(--letter-spacing-wider);
    color: var(--primary-light);
    opacity: 0;
  }

  /* Title */
  .hero-title {
    margin-bottom: var(--space-xl);
    overflow: hidden;
    text-align: center;
  }

  .hero-title-line {
    display: block;
    overflow: hidden;
    position: relative;
    text-align: center;
  }

  .hero-title-text {
    display: inline-block;
    font-size: clamp(3.2rem, 10vw, 7rem);
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: var(--letter-spacing-tight);
    color: var(--text-primary);
    opacity: 0;
    transform: translateY(100%);
    will-change: transform, opacity;
  }
  
  /* Split Text Characters */
  .char {
    display: inline-block;
    opacity: 0;
    transform: translateY(100%);
    will-change: transform, opacity;
  }
  
  .char-space {
    width: 0.3em;
  }

  .hero-title-accent {
    color: var(--primary-light);
  }
  
  /* Glitch Effect on Accent */
  .hero-title-accent[data-glitch]::before,
  .hero-title-accent[data-glitch]::after {
    content: attr(data-text);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    color: inherit;
  }
  
  .hero-title-accent[data-glitch]::before {
    left: 2px;
    text-shadow: -2px 0 #ff0080;
    clip-path: inset(0 0 0 0);
    animation: glitch-1 2s infinite linear alternate-reverse;
  }
  
  .hero-title-accent[data-glitch]::after {
    left: -2px;
    text-shadow: 2px 0 #00ffff;
    clip-path: inset(0 0 0 0);
    animation: glitch-2 2s infinite linear alternate-reverse;
  }
  
  @keyframes glitch-1 {
    0%, 100% { clip-path: inset(0 0 95% 0); }
    20% { clip-path: inset(20% 0 60% 0); }
    40% { clip-path: inset(40% 0 40% 0); }
    60% { clip-path: inset(60% 0 20% 0); }
    80% { clip-path: inset(80% 0 5% 0); }
  }
  
  @keyframes glitch-2 {
    0%, 100% { clip-path: inset(95% 0 0 0); }
    20% { clip-path: inset(60% 0 20% 0); }
    40% { clip-path: inset(40% 0 40% 0); }
    60% { clip-path: inset(20% 0 60% 0); }
    80% { clip-path: inset(5% 0 80% 0); }
  }

  /* Subtitle */
  .hero-subtitle {
    margin-bottom: var(--space-2xl);
    overflow: hidden;
  }

  .hero-subtitle-text {
    display: inline-block;
    font-size: var(--font-size-lg);
    font-weight: 300;
    line-height: var(--line-height-relaxed);
    color: #fff;
    opacity: 0;
    transform: translateY(30px);
  }

  /* CTA */
  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-md);
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-cta-btn {
    position: relative;
    overflow: hidden;
    will-change: transform;
  }
  
  .btn-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.2) 50%,
      transparent 100%
    );
    transform: skewX(-25deg);
    pointer-events: none;
  }
  
  .hero-cta-btn:hover .btn-shine {
    animation: btnShine 0.6s ease-out;
  }
  
  @keyframes btnShine {
    0% { left: -100%; }
    100% { left: 200%; }
  }

  .hero-cta-btn svg {
    transition: transform var(--transition-base);
  }

  .hero-cta-btn:hover svg {
    transform: translateX(4px);
  }

  /* Stats Badge */
  .hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-lg);
    margin-top: var(--space-xl);
    padding: var(--space-lg) var(--space-xl);
    background: rgba(26, 54, 93, 0.15);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-stat {
    text-align: center;
    position: relative;
  }

  .hero-stat-number {
    display: inline-block;
    font-size: var(--font-size-3xl);
    font-weight: 600;
    line-height: 1;
    color: var(--text-primary);
    font-variant-numeric: tabular-nums;
  }
  
  .hero-stat-suffix {
    display: inline-block;
    font-size: var(--font-size-xl);
    font-weight: 600;
    color: var(--primary-light);
    margin-left: 2px;
  }

  .hero-stat-label {
    display: block;
    margin-top: var(--space-xs);
    font-size: var(--font-size-xs);
    font-weight: 500;
    letter-spacing: var(--letter-spacing-wider);
    color: var(--text-muted);
  }

  .hero-stat-divider {
    width: 1px;
    height: 40px;
    background: linear-gradient(
      180deg,
      transparent 0%,
      var(--border-light) 50%,
      transparent 100%
    );
  }

  /* Hero Slides */
  .hero-slides {
    position: relative;
    width: 100%;
  }

  .hero-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: visibility 0s linear 0.6s;
  }

  .hero-slide.is-active {
    position: relative;
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transition: visibility 0s linear 0s;
  }

  /* Hero Indicators */
  .hero-indicators {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    margin-top: var(--space-2xl);
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-indicator {
    position: relative;
    width: 48px;
    height: 3px;
    background: rgba(255, 255, 255, 0.2);
    border: none;
    border-radius: var(--radius-full);
    cursor: pointer;
    overflow: hidden;
    padding: 0;
    transition: background 0.3s ease;
  }

  .hero-indicator:hover {
    background: rgba(255, 255, 255, 0.35);
  }

  .hero-indicator-progress {
    position: absolute;
    top: 0;
    left: 0;
    width: 0%;
    height: 100%;
    background: var(--primary-light);
    border-radius: var(--radius-full);
    transition: width 0s;
  }

  .hero-indicator.is-active .hero-indicator-progress {
    width: 0%;
  }

  .hero-indicator.is-done .hero-indicator-progress {
    width: 100%;
    transition: none;
  }

  /* Scroll Indicator */
  .scroll-indicator {
    position: absolute;
    left: 50%;
    bottom: var(--space-2xl);
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    opacity: 0;
    cursor: pointer;
    transition: opacity var(--transition-base);
  }

  .scroll-indicator:hover {
    opacity: 0.8;
  }

  .scroll-indicator-text {
    font-size: var(--font-size-xs);
    font-weight: 500;
    letter-spacing: var(--letter-spacing-wider);
    color: var(--text-muted);
  }

  .scroll-indicator-line {
    position: relative;
    width: 1px;
    height: 50px;
    background: rgba(255, 255, 255, 0.2);
    overflow: hidden;
  }

  .scroll-indicator-dot {
    position: absolute;
    top: 0;
    left: 50%;
    width: 3px;
    height: 10px;
    background: var(--primary-light);
    border-radius: var(--radius-full);
    transform: translateX(-50%);
    animation: scrollDot 2s ease-in-out infinite;
  }

  .scroll-indicator-arrow {
    color: var(--text-muted);
    animation: scrollBounce 2s ease-in-out infinite;
  }

  @keyframes scrollDot {
    0%, 100% {
      top: 0;
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      top: 100%;
      opacity: 0;
    }
  }

  @keyframes scrollBounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(6px);
    }
  }
  
  /* Animated Gradient Shift */
  @keyframes gradientShift {
    0%, 100% {
      transform: translate(0, 0) rotate(0deg);
    }
    33% {
      transform: translate(5%, 5%) rotate(120deg);
    }
    66% {
      transform: translate(-5%, 3%) rotate(240deg);
    }
  }

  /* Responsive */
  @media (max-width: 1023px) {
    .geo-shape--1 { width: 200px; height: 200px; }
    .geo-shape--2 { width: 150px; height: 150px; }
    .geo-shape--3 { width: 100px; height: 100px; }
    .geo-shape--4 { width: 120px; height: 120px; }
    .geo-shape--5 { width: 80px; height: 80px; }
    
    .brand-line--left { left: 5%; }
    .brand-line--right { right: 5%; }
  }

  @media (max-width: 719px) {
    .hero {
      min-height: 100vh;
      min-height: 100dvh;
    }

    .hero-content {
      padding-top: calc(var(--header-height) + var(--space-xl));
    }

    .hero-title-text {
      font-size: clamp(2rem, 10vw, 3rem);
    }

    .hero-subtitle-text {
      font-size: var(--font-size-base);
    }

    .hero-cta {
      flex-direction: column;
    }

    .hero-cta-btn {
      width: 100%;
      justify-content: center;
    }

    .hero-stats {
      margin-top: var(--space-2xl);
      width: fit-content;
    }

    .scroll-indicator {
      bottom: var(--space-lg);
    }

    .hero-indicators {
      margin-top: var(--space-xl);
    }

    .hero-indicator {
      width: 36px;
    }

    .hero-video {
      display: none;
    }

    .hero-video-fallback {
      display: block;
    }

    .hero-geometrics,
    .hero-mouse-light,
    .brand-line {
      display: none;
    }
    
    .hero-particles .particle:nth-child(n+15) {
      display: none;
    }
  }

  /* Reduced Motion */
  @media (prefers-reduced-motion: reduce) {
    .scroll-indicator-dot,
    .scroll-indicator-arrow {
      animation: none;
    }
    
    .hero-eyebrow,
    .hero-title-text,
    .hero-subtitle-text,
    .hero-cta,
    .hero-stats,
    .hero-indicators,
    .scroll-indicator,
    .geo-shape,
    .particle,
    .brand-line,
    .hero-gradient-layer,
    .char {
      opacity: 1 !important;
      transform: none !important;
      animation: none !important;
    }
    
    .hero-eyebrow-line {
      width: 40px !important;
    }
    
    .hero-eyebrow-text {
      opacity: 1 !important;
    }
    
    .hero-bg-wrapper,
    .hero-bg-image {
      transform: none !important;
    }
    
    .hero-mouse-light {
      display: none;
    }

    .hero-video {
      display: none;
    }

    .hero-video-fallback {
      display: block;
    }

    .btn-shine {
      display: none;
    }
    
    .hero-title-accent[data-glitch]::before,
    .hero-title-accent[data-glitch]::after {
      display: none;
    }
  }
</style>

<script>
  // Premium GSAP Hero Animation with Carousel, Parallax, Mouse Tracking & Particles
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  // Register ScrollTrigger plugin
  gsap.registerPlugin(ScrollTrigger);

  // Check for reduced motion preference
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  // Prevent duplicate initialization
  let heroInitialized = false;

  function initHeroAnimation() {
    if (heroInitialized) return;
    heroInitialized = true;

    const hero = document.querySelector('[data-hero]') as HTMLElement;
    if (!hero || prefersReducedMotion) return;

    // Elements
    const bgWrapper = hero.querySelector('[data-hero-bg-wrapper]') as HTMLElement;
    const gradientLayer = hero.querySelector('[data-hero-gradient]') as HTMLElement;
    const geoShapes = hero.querySelectorAll('.geo-shape');
    const particles = hero.querySelectorAll('.particle');
    const mouseLight = hero.querySelector('[data-hero-mouse-light]') as HTMLElement;
    const brandLines = hero.querySelectorAll('.brand-line');
    const stats = hero.querySelector('[data-hero-stats]') as HTMLElement;
    const counters = hero.querySelectorAll('[data-counter]');
    const scrollIndicator = hero.querySelector('[data-scroll-indicator]') as HTMLElement;
    const heroContent = hero.querySelector('[data-hero-content]') as HTMLElement;

    // Carousel elements
    const slides = hero.querySelectorAll('[data-hero-slide]') as NodeListOf<HTMLElement>;
    const indicators = hero.querySelectorAll('[data-hero-indicator]') as NodeListOf<HTMLElement>;
    const indicatorsContainer = hero.querySelector('[data-hero-indicators]') as HTMLElement;
    const heroVideos = hero.querySelectorAll('[data-hero-video]') as NodeListOf<HTMLVideoElement>;
    const heroFallback = hero.querySelector('[data-hero-fallback]') as HTMLImageElement;

    // Poster images per slide for mobile fallback
    const posterImages = [
      '/videos/hero1-poster.jpg',
      '/videos/hero2-poster.jpg',
      '/videos/hero3-poster.jpg',
      '/videos/hero4-poster.jpg',
    ];

    const SLIDE_COUNT = slides.length;
    const AUTO_PLAY_INTERVAL = 6000; // 6 seconds per slide
    let currentSlide = 0;
    let autoPlayTimer: ReturnType<typeof setTimeout> | null = null;
    let progressTween: gsap.core.Tween | null = null;
    let isTransitioning = false;

    // Astro 페이지 전환 후 autoplay 속성이 재트리거되지 않으므로 명시적으로 재생
    heroVideos[0]?.play().catch(() => {});
    let initialAnimationDone = false;

    // =====================
    // SPLIT TEXT SETUP (for all slides)
    // =====================

    slides.forEach((slide) => {
      const titleTexts = slide.querySelectorAll('[data-split-text]');
      titleTexts.forEach((textEl) => {
        const el = textEl as HTMLElement;
        const text = el.textContent || '';
        el.innerHTML = '';

        text.split('').forEach((char) => {
          const span = document.createElement('span');
          if (char === ' ') {
            span.className = 'char char-space';
            span.innerHTML = '&nbsp;';
          } else {
            span.className = 'char';
            span.textContent = char;
          }
          el.appendChild(span);
        });

        gsap.set(el, { opacity: 1, y: 0 });
      });

      // Set glitch data on accent texts
      const accentText = slide.querySelector('[data-accent]') as HTMLElement;
      if (accentText) {
        accentText.setAttribute('data-text', accentText.textContent || '');
      }
    });

    // =====================
    // SLIDE ANIMATION FUNCTIONS
    // =====================

    function animateSlideIn(slide: HTMLElement, isInitial = false) {
      const contentInner = slide.querySelector('.hero-content-inner') as HTMLElement;
      const eyebrow = slide.querySelector('[data-hero-eyebrow]') as HTMLElement;
      const eyebrowLine = slide.querySelector('.hero-eyebrow-line') as HTMLElement;
      const eyebrowText = slide.querySelector('.hero-eyebrow-text') as HTMLElement;
      const chars = slide.querySelectorAll('.char');
      const accentText = slide.querySelector('[data-accent]') as HTMLElement;
      const subtitleText = slide.querySelector('.hero-subtitle-text') as HTMLElement;
      const cta = slide.querySelector('[data-hero-cta]') as HTMLElement;

      // Reset elements — content-inner starts offset and slides to center
      if (contentInner) gsap.set(contentInner, { opacity: 0, y: 60, scale: 0.97 });
      gsap.set(eyebrow, { opacity: 0, y: 20 });
      gsap.set(eyebrowLine, { width: 0 });
      gsap.set(eyebrowText, { opacity: 0 });
      gsap.set(chars, { opacity: 0, y: '100%' });
      gsap.set(subtitleText, { opacity: 0, y: 30 });
      gsap.set(cta, { opacity: 0, y: 20 });

      const tl = gsap.timeline({
        delay: isInitial ? 0.5 : 0.1,
        onComplete: () => {
          isTransitioning = false;
        }
      });

      // Content-inner slides up to center position
      if (contentInner) {
        tl.to(contentInner, {
          opacity: 1, y: 0, scale: 1,
          duration: 0.8, ease: 'power3.out',
        }, 0);
      }

      // Eyebrow
      tl.to(eyebrow, { opacity: 1, y: 0, duration: 0.6, ease: 'power3.out' }, 0.15)
        .to(eyebrowLine, { width: 40, duration: 0.5, ease: 'power2.inOut' }, 0.25)
        .to(eyebrowText, { opacity: 1, duration: 0.4 }, 0.4);

      // Characters
      tl.to(chars, {
        opacity: 1,
        y: 0,
        duration: 0.6,
        stagger: { amount: 0.6, from: 'start' },
        ease: 'power4.out',
      }, 0.3);

      // Glitch effect
      if (accentText) {
        tl.call(() => {
          accentText.setAttribute('data-glitch', 'true');
          setTimeout(() => accentText.removeAttribute('data-glitch'), 1500);
        }, [], 1.2);
      }

      // Subtitle
      tl.to(subtitleText, { opacity: 1, y: 0, duration: 0.6, ease: 'power3.out' }, '-=0.3');

      // CTA
      tl.to(cta, { opacity: 1, y: 0, duration: 0.6, ease: 'power3.out' }, '-=0.3');

      return tl;
    }

    function animateSlideOut(slide: HTMLElement) {
      const contentInner = slide.querySelector('.hero-content-inner') as HTMLElement;
      const eyebrow = slide.querySelector('[data-hero-eyebrow]') as HTMLElement;
      const chars = slide.querySelectorAll('.char');
      const subtitleText = slide.querySelector('.hero-subtitle-text') as HTMLElement;
      const cta = slide.querySelector('[data-hero-cta]') as HTMLElement;

      const tl = gsap.timeline();

      // Content-inner slides up and fades
      if (contentInner) {
        tl.to(contentInner, {
          opacity: 0, y: -40, scale: 0.98,
          duration: 0.5, ease: 'power2.in',
        }, 0);
      }

      tl.to([cta, subtitleText], {
        opacity: 0,
        y: -20,
        duration: 0.35,
        ease: 'power2.in',
        stagger: 0.05,
      }, 0);

      tl.to(chars, {
        opacity: 0,
        y: '-60%',
        duration: 0.35,
        stagger: { amount: 0.2, from: 'end' },
        ease: 'power2.in',
      }, 0.05);

      tl.to(eyebrow, {
        opacity: 0,
        y: -15,
        duration: 0.3,
        ease: 'power2.in',
      }, 0.1);

      return tl;
    }

    // =====================
    // CAROUSEL CONTROLLER
    // =====================

    function switchVideo(index: number) {
      // Crossfade videos
      heroVideos.forEach((video, i) => {
        if (i === index) {
          video.classList.add('is-active');
          video.play().catch(() => {});
        } else {
          video.classList.remove('is-active');
          // Pause non-active videos after transition to save resources
          setTimeout(() => {
            if (!video.classList.contains('is-active')) {
              video.pause();
            }
          }, 900);
        }
      });

      // Update mobile fallback poster
      if (heroFallback) {
        heroFallback.src = posterImages[index] || posterImages[0];
      }
    }

    function goToSlide(index: number) {
      if (isTransitioning || index === currentSlide) return;
      isTransitioning = true;

      const oldSlide = slides[currentSlide];
      const newSlide = slides[index];

      // Update indicators
      indicators.forEach((ind, i) => {
        ind.classList.remove('is-active', 'is-done');
        if (i < index) ind.classList.add('is-done');
      });
      indicators[index].classList.add('is-active');

      // Switch background video
      switchVideo(index);

      // Animate out old slide, then swap and animate in new
      const outTl = animateSlideOut(oldSlide);
      outTl.call(() => {
        oldSlide.classList.remove('is-active');
        newSlide.classList.add('is-active');
        currentSlide = index;
        animateSlideIn(newSlide);
      });

      // Restart progress animation
      startProgress(index);
    }

    function nextSlide() {
      const next = (currentSlide + 1) % SLIDE_COUNT;
      goToSlide(next);
    }

    // =====================
    // PROGRESS BAR & AUTO-PLAY
    // =====================

    function startProgress(index: number) {
      // Kill previous progress
      if (progressTween) progressTween.kill();
      if (autoPlayTimer) clearTimeout(autoPlayTimer);

      // Reset all progress bars
      indicators.forEach((ind) => {
        const bar = ind.querySelector('.hero-indicator-progress') as HTMLElement;
        gsap.set(bar, { width: '0%' });
      });

      // Set completed indicators
      indicators.forEach((ind, i) => {
        if (i < index) {
          const bar = ind.querySelector('.hero-indicator-progress') as HTMLElement;
          gsap.set(bar, { width: '100%' });
        }
      });

      // Animate current progress bar
      const currentBar = indicators[index].querySelector('.hero-indicator-progress') as HTMLElement;
      progressTween = gsap.fromTo(currentBar,
        { width: '0%' },
        {
          width: '100%',
          duration: AUTO_PLAY_INTERVAL / 1000,
          ease: 'none',
          onComplete: nextSlide,
        }
      );
    }

    function startAutoPlay() {
      startProgress(currentSlide);
    }

    function pauseAutoPlay() {
      if (progressTween) progressTween.pause();
    }

    function resumeAutoPlay() {
      if (progressTween) progressTween.resume();
    }

    // =====================
    // INDICATOR CLICK HANDLERS
    // =====================

    indicators.forEach((indicator, i) => {
      indicator.addEventListener('click', () => {
        if (i !== currentSlide && !isTransitioning) {
          goToSlide(i);
        }
      });
    });

    // Pause on hover
    hero.addEventListener('mouseenter', pauseAutoPlay);
    hero.addEventListener('mouseleave', () => {
      if (initialAnimationDone) resumeAutoPlay();
    });

    // =====================
    // 1. BACKGROUND ZOOM & PARALLAX
    // =====================

    gsap.fromTo(bgWrapper,
      { scale: 1.2 },
      { scale: 1, duration: 2.5, ease: 'power2.out' }
    );

    gsap.to(bgWrapper, {
      yPercent: 30,
      ease: 'none',
      scrollTrigger: {
        trigger: hero,
        start: 'top top',
        end: 'bottom top',
        scrub: 1,
      }
    });

    gsap.to(heroContent, {
      yPercent: -15,
      ease: 'none',
      scrollTrigger: {
        trigger: hero,
        start: 'top top',
        end: 'bottom top',
        scrub: 1,
      }
    });

    // =====================
    // 2. ANIMATED GRADIENT BACKGROUND
    // =====================

    gsap.to(gradientLayer, { opacity: 1, duration: 2, delay: 0.5 });
    gsap.to(gradientLayer, { rotation: 360, duration: 60, ease: 'none', repeat: -1 });
    gsap.to(gradientLayer, { x: '5%', y: '3%', duration: 15, ease: 'sine.inOut', yoyo: true, repeat: -1 });

    // =====================
    // 3. FLOATING GEOMETRIC ELEMENTS
    // =====================

    const geoEntranceTl = gsap.timeline({
      delay: 1.5,
      onComplete: () => startFloatingAnimations(),
    });

    geoEntranceTl.to(geoShapes, {
      opacity: 1, scale: 1, duration: 0.8, stagger: 0.2, ease: 'back.out(1.7)',
    });

    function startFloatingAnimations() {
      geoShapes.forEach((shape, i) => {
        gsap.to(shape, {
          y: `${-30 + i * 10}`, x: `${15 - i * 5}`, rotation: 360,
          duration: 20 + i * 5, ease: 'none', repeat: -1,
        });
        gsap.to(shape, {
          scale: 1.1, duration: 4 + i, ease: 'sine.inOut', yoyo: true, repeat: -1,
        });
      });
    }

    // =====================
    // 4. PARTICLE SYSTEM
    // =====================

    particles.forEach((particle, i) => {
      const el = particle as HTMLElement;
      gsap.set(el, { left: `${Math.random() * 100}%`, top: `${Math.random() * 100}%` });
      gsap.to(el, { opacity: 0.3 + Math.random() * 0.4, duration: 1, delay: 2 + i * 0.1 });
      gsap.to(el, {
        y: `-=${50 + Math.random() * 100}`, x: `+=${-50 + Math.random() * 100}`,
        duration: 10 + Math.random() * 20, ease: 'none', repeat: -1, yoyo: true, delay: Math.random() * 5,
      });
      gsap.to(el, {
        opacity: 0.1, duration: 1 + Math.random() * 2, ease: 'sine.inOut', yoyo: true, repeat: -1, delay: Math.random() * 3,
      });
    });

    // =====================
    // 5. MOUSE FOLLOWING INTERACTION
    // =====================

    let mouseX = window.innerWidth / 2;
    let mouseY = window.innerHeight / 2;
    let currentX = mouseX;
    let currentY = mouseY;

    gsap.to(mouseLight, { opacity: 1, duration: 1, delay: 2 });

    const updateMouseLight = () => {
      currentX += (mouseX - currentX) * 0.08;
      currentY += (mouseY - currentY) * 0.08;
      gsap.set(mouseLight, { x: currentX, y: currentY });
      requestAnimationFrame(updateMouseLight);
    };
    updateMouseLight();

    hero.addEventListener('mousemove', (e) => {
      mouseX = e.clientX;
      mouseY = e.clientY;
      geoShapes.forEach((shape, i) => {
        const depth = 0.02 + i * 0.01;
        gsap.to(shape, {
          x: (e.clientX - window.innerWidth / 2) * depth,
          y: (e.clientY - window.innerHeight / 2) * depth,
          duration: 1, ease: 'power2.out',
        });
      });
    });

    // Magnetic buttons (for all slides)
    const allMagneticBtns = hero.querySelectorAll('[data-magnetic]');
    allMagneticBtns.forEach((btn) => {
      const button = btn as HTMLElement;
      button.addEventListener('mousemove', (e: MouseEvent) => {
        const rect = button.getBoundingClientRect();
        gsap.to(button, {
          x: (e.clientX - rect.left - rect.width / 2) * 0.2,
          y: (e.clientY - rect.top - rect.height / 2) * 0.2,
          duration: 0.3, ease: 'power2.out',
        });
      });
      button.addEventListener('mouseleave', () => {
        gsap.to(button, { x: 0, y: 0, duration: 0.5, ease: 'elastic.out(1, 0.5)' });
      });
    });

    // =====================
    // 6. INITIAL ENTRANCE TIMELINE
    // =====================

    const tl = gsap.timeline({
      defaults: { ease: 'power3.out', duration: 1 },
      delay: 0.3,
    });

    // Brand lines
    tl.to(brandLines, { opacity: 0.5, duration: 1.5, stagger: 0.2 }, 0);

    // Animate first slide content
    const firstSlide = slides[0];
    const firstContentInner = firstSlide.querySelector('.hero-content-inner') as HTMLElement;
    const firstEyebrow = firstSlide.querySelector('[data-hero-eyebrow]') as HTMLElement;
    const firstEyebrowLine = firstSlide.querySelector('.hero-eyebrow-line') as HTMLElement;
    const firstEyebrowText = firstSlide.querySelector('.hero-eyebrow-text') as HTMLElement;
    const firstChars = firstSlide.querySelectorAll('.char');
    const firstAccent = firstSlide.querySelector('[data-accent]') as HTMLElement;
    const firstSubtitle = firstSlide.querySelector('.hero-subtitle-text') as HTMLElement;
    const firstCta = firstSlide.querySelector('[data-hero-cta]') as HTMLElement;

    // Content-inner slide up to center
    if (firstContentInner) {
      gsap.set(firstContentInner, { opacity: 0, y: 60, scale: 0.97 });
      tl.to(firstContentInner, {
        opacity: 1, y: 0, scale: 1,
        duration: 1, ease: 'power3.out',
      }, 0.4);
    }

    // Eyebrow
    tl.to(firstEyebrow, { opacity: 1, y: 0, duration: 0.8 }, 0.5)
      .to(firstEyebrowLine, { width: 40, duration: 0.6, ease: 'power2.inOut' }, 0.6)
      .to(firstEyebrowText, { opacity: 1, duration: 0.5 }, 0.9);

    // Characters
    tl.to(firstChars, {
      opacity: 1, y: 0, duration: 0.8,
      stagger: { amount: 0.8, from: 'start' },
      ease: 'power4.out',
    }, 1);

    // Glitch
    if (firstAccent) {
      tl.call(() => {
        firstAccent.setAttribute('data-glitch', 'true');
        setTimeout(() => firstAccent.removeAttribute('data-glitch'), 2000);
      }, [], 2.2);
    }

    // Subtitle
    tl.to(firstSubtitle, { opacity: 1, y: 0, duration: 0.8 }, '-=0.4');

    // CTA
    tl.to(firstCta, { opacity: 1, y: 0, duration: 0.8 }, '-=0.4');

    // Stats (centered, slide up)
    tl.to(stats, { opacity: 1, y: 0, duration: 0.8 }, '-=0.4');

    // Counter animation
    counters.forEach((counter) => {
      const el = counter as HTMLElement;
      const target = parseInt(el.dataset.counter || '0');
      gsap.to({ value: 0 }, {
        value: target, duration: 2, delay: 2.5, ease: 'power2.out',
        onUpdate: function() {
          const current = Math.round(this.targets()[0].value);
          el.textContent = target >= 1000 ? (current / 1000).toFixed(1) + 'K' : current.toString();
        },
      });
    });

    // Indicators
    tl.to(indicatorsContainer, { opacity: 1, y: 0, duration: 0.6 }, '-=0.3');

    // Scroll indicator
    tl.to(scrollIndicator, { opacity: 1, duration: 0.6 }, '-=0.2');

    // Start auto-play after initial animation completes
    tl.call(() => {
      initialAnimationDone = true;
      startAutoPlay();
    });

    // =====================
    // 7. SCROLL INTERACTIONS
    // =====================

    ScrollTrigger.create({
      trigger: hero,
      start: 'top top',
      end: '50% top',
      scrub: 1,
      onUpdate: (self) => {
        const progress = self.progress;
        const activeSlide = slides[currentSlide];
        const activeEyebrow = activeSlide?.querySelector('[data-hero-eyebrow]');
        const activeCta = activeSlide?.querySelector('[data-hero-cta]');
        const activeChars = activeSlide?.querySelectorAll('.char');

        gsap.set([activeEyebrow, activeCta, stats, scrollIndicator, indicatorsContainer].filter(Boolean), {
          opacity: 1 - progress * 1.5,
        });
        if (activeChars) {
          gsap.set(activeChars, { opacity: 1 - progress * 1.2 });
        }
      },
    });

    scrollIndicator?.addEventListener('click', () => {
      const nextSection = document.querySelector('#about, #services, .section');
      if (nextSection) nextSection.scrollIntoView({ behavior: 'smooth' });
    });
  }

  // Cleanup function — only kill Hero-scoped ScrollTriggers and tweens
  function cleanupHeroAnimation() {
    heroInitialized = false;
    const hero = document.querySelector('[data-hero]') as HTMLElement;
    if (!hero) return;

    // Kill only ScrollTriggers whose trigger is within the hero section
    ScrollTrigger.getAll().forEach(trigger => {
      const triggerEl = trigger.vars?.trigger;
      if (triggerEl && hero.contains(triggerEl as Node)) {
        trigger.kill();
      } else if (triggerEl === hero) {
        trigger.kill();
      }
    });

    // Kill tweens on hero-specific elements only
    const heroElements = hero.querySelectorAll('*');
    heroElements.forEach(el => gsap.killTweensOf(el));
    gsap.killTweensOf(hero);
  }

  document.addEventListener('DOMContentLoaded', initHeroAnimation);
  document.addEventListener('astro:page-load', initHeroAnimation);
  document.addEventListener('astro:before-preparation', cleanupHeroAnimation);
</script>

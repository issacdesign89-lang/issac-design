---
/**
 * ShopFooter Component
 * Footer for shop pages with links, company info, and business details
 */

import { getProducts } from '../../lib/data-fetchers';
import { APP_VERSION } from '../../version';

const { categories } = await getProducts();
const currentYear = new Date().getFullYear();
const version = APP_VERSION;
---

<footer class="shop-footer">
  <div class="shop-footer__container">
    <!-- Top Section -->
    <div class="shop-footer__top">
      <!-- Brand -->
      <div class="shop-footer__brand">
        <a href="/shop" class="shop-footer__logo">
          <span class="shop-footer__logo-text">issac.design</span>
          <span class="shop-footer__logo-sub">Shop</span>
        </a>
        <p class="shop-footer__tagline">
          감각적인 디자인으로 브랜드를 빛내는 사이니지 스튜디오.<br />
          LED 채널간판부터 네온사인까지, 맞춤 제작합니다.
        </p>
        <!-- Contact Quick -->
        <div class="shop-footer__contact-quick">
          <a href="tel:010-1234-5678" class="shop-footer__contact-item">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M14.67 11.37v2a1.33 1.33 0 01-1.45 1.33 13.16 13.16 0 01-5.74-2.04 12.97 12.97 0 01-4-4 13.16 13.16 0 01-2.04-5.76A1.33 1.33 0 012.77 1.47h2a1.33 1.33 0 011.33 1.15c.08.63.24 1.24.46 1.83a1.33 1.33 0 01-.3 1.4l-.85.85a10.67 10.67 0 004 4l.85-.85a1.33 1.33 0 011.4-.3c.59.22 1.2.38 1.83.46a1.33 1.33 0 011.14 1.36z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span>010-1234-5678</span>
          </a>
          <a href="mailto:info@issac.design" class="shop-footer__contact-item">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M2.67 2.67h10.66c.74 0 1.34.6 1.34 1.33v8c0 .73-.6 1.33-1.34 1.33H2.67c-.74 0-1.34-.6-1.34-1.33V4c0-.73.6-1.33 1.34-1.33z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M14.67 4L8 8.67 1.33 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span>info@issac.design</span>
          </a>
        </div>
      </div>

      <!-- Links Grid -->
      <div class="shop-footer__links-grid">
        <!-- Categories -->
        <div class="shop-footer__links-col">
          <h4 class="shop-footer__links-title">카테고리</h4>
          <ul class="shop-footer__links">
            {categories.map(cat => (
              <li>
                <a href={`/shop/category/${cat.id}`} class="shop-footer__link">{cat.name}</a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Customer Service -->
        <div class="shop-footer__links-col">
          <h4 class="shop-footer__links-title">고객 서비스</h4>
          <ul class="shop-footer__links">
            <li><a href="/shop/faq" class="shop-footer__link">자주 묻는 질문</a></li>
            <li><a href="/shop/about" class="shop-footer__link">회사 소개</a></li>
            <li><a href="/shop/contact" class="shop-footer__link">고객센터</a></li>
          </ul>
        </div>

        <!-- Business Info -->
        <div class="shop-footer__links-col">
          <h4 class="shop-footer__links-title">영업 시간</h4>
          <div class="shop-footer__business-hours">
            <div class="shop-footer__hours-row">
              <span class="shop-footer__hours-label">평일</span>
              <span class="shop-footer__hours-value">09:00 - 18:00</span>
            </div>
            <div class="shop-footer__hours-row">
              <span class="shop-footer__hours-label">토요일</span>
              <span class="shop-footer__hours-value">10:00 - 14:00</span>
            </div>
            <div class="shop-footer__hours-row">
              <span class="shop-footer__hours-label">일/공휴일</span>
              <span class="shop-footer__hours-value">휴무</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom Section -->
    <div class="shop-footer__bottom">
      <div class="shop-footer__business-info">
        <p>상호: (주)이삭디자인 | 대표: 홍길동 | 사업자등록번호: 123-45-67890</p>
        <p>주소: 서울특별시 강남구 테헤란로 123, 4층 | 통신판매업 신고번호: 제2024-서울강남-12345호</p>
      </div>
      <p class="shop-footer__copyright">
        &copy; {currentYear} issac.design. All rights reserved.
      </p>
      <p class="shop-footer__version">v{version}</p>
    </div>
  </div>
</footer>

<style>
  .shop-footer {
    position: relative;
    width: 100%;
    background: var(--background);
    border-top: 1px solid var(--border);
    padding: var(--space-4xl) 0 var(--space-xl);
    /* PWA safe-area: 홈 인디케이터 */
    padding-bottom: calc(var(--space-xl) + env(safe-area-inset-bottom, 0px));
  }

  .shop-footer__container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  /* Top */
  .shop-footer__top {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--space-4xl);
    padding-bottom: var(--space-3xl);
    border-bottom: 1px solid var(--border);
  }

  /* Brand */
  .shop-footer__logo {
    display: inline-flex;
    align-items: baseline;
    gap: var(--space-sm);
    margin-bottom: var(--space-lg);
  }

  .shop-footer__logo-text {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--text-primary);
    letter-spacing: var(--letter-spacing-tight);
  }

  .shop-footer__logo-sub {
    font-size: var(--font-size-sm);
    color: var(--primary);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
  }

  .shop-footer__tagline {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-lg);
  }

  .shop-footer__contact-quick {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .shop-footer__contact-item {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    transition: color var(--transition-base);
  }

  .shop-footer__contact-item:hover {
    color: var(--primary);
  }

  .shop-footer__contact-item svg {
    flex-shrink: 0;
  }

  /* Links Grid */
  .shop-footer__links-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
  }

  .shop-footer__links-title {
    font-size: var(--font-size-sm);
    font-weight: 600;
    color: var(--text-primary);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wide);
    margin-bottom: var(--space-lg);
  }

  .shop-footer__links {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .shop-footer__link {
    font-size: var(--font-size-sm);
    color: var(--text-muted);
    transition: color var(--transition-base);
  }

  .shop-footer__link:hover {
    color: var(--primary);
  }

  /* Business Hours */
  .shop-footer__business-hours {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .shop-footer__hours-row {
    display: flex;
    justify-content: space-between;
    gap: var(--space-md);
  }

  .shop-footer__hours-label {
    font-size: var(--font-size-sm);
    color: var(--text-muted);
  }

  .shop-footer__hours-value {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    font-weight: 500;
  }

  /* Bottom */
  .shop-footer__bottom {
    padding-top: var(--space-xl);
    text-align: center;
  }

  .shop-footer__business-info {
    margin-bottom: var(--space-md);
  }

  .shop-footer__business-info p {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
    line-height: var(--line-height-relaxed);
  }

  .shop-footer__copyright {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
  }

  .shop-footer__version {
    font-size: 10px;
    color: var(--text-muted);
    opacity: 0.4;
    margin-top: var(--space-xs);
  }

  /* Responsive */
  @media (max-width: 1023px) {
    .shop-footer__top {
      grid-template-columns: 1fr;
      gap: var(--space-3xl);
    }
  }

  @media (max-width: 719px) {
    .shop-footer {
      padding: var(--space-3xl) 0 var(--space-lg);
    }

    .shop-footer__links-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .shop-footer__hours-row {
      gap: var(--space-xl);
    }
  }
</style>
